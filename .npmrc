# 禁用 pnpm 的 minimumReleaseAge 供应链熔断策略。
# CI runner(尤其 withastro/action)默认开启该策略,会拦截发布不足 24h 的依赖,
# 导致 lockfile 校验失败。本项目依赖来源可信,关闭以避免误伤。
minimumReleaseAge=0

# 允许所有依赖运行构建脚本(esbuild 需 postinstall 安装平台二进制)。
# CI 上的 pnpm 默认会因"忽略构建脚本"以非零退出码失败,设此开关避免该阻断。
dangerouslyAllowAllBuilds=true
